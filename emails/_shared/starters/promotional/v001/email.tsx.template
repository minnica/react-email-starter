import { CtaSection } from "@email/kits/v001";
import { PromotionalLayout } from "@email/layouts/promotional/v001";
import type { CtaContent } from "@email/types/email";
import { @BRAND_EXPORT@ } from "@BRAND_IMPORT@";

const content = {
  headingLevel: "h2",
  title: @SUBJECT_JSON@,
  description: "Sustituye este texto por el contenido aprobado de la campaña.",
  direction: "vertical",
  primaryButton: {
    text: "Conoce más",
    href: "https://example.com",
  },
} satisfies CtaContent;

export function Email() {
  return (
    <PromotionalLayout brand={@BRAND_EXPORT@} preheader={@PREHEADER_JSON@}>
      <CtaSection brand={@BRAND_EXPORT@} content={content} />
    </PromotionalLayout>
  );
}

export default Email;
